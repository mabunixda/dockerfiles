image: docker

pipelines:
  default:
    - step:
        name: build dockerfiles
        script:
          - $BITBUCKET_CLONE_DIR/build_changes.sh
        services:
          - docker

options:
  docker: true
